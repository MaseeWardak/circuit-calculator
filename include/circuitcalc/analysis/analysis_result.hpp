#pragma once

#include <cstddef>

/// Filled in as DC analysis is implemented.
struct DcAnalysisResult {
    std::size_t num_nodes{0};
    // TODO: node voltages, floating voltage source currents, etc.
};
